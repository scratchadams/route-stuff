protocol kernel {
    import none;
    export all;
}

protocol device {
}

protocol direct {
    interface "eth0";
}

protocol bgp {
    import all;
    export all;

    local 10.3.3.1 as 3001;
    neighbor 10.3.2.1 as 2002;
}
